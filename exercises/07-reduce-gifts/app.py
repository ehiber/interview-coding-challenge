
def reduce_gifts(prices=[], k=3, threshold=14):
    """
    Stub function for students to implement.

    Given a list of integers `prices`, an integer `k` and an integer `threshold`,
    return the minimum number of items that need to be removed so that the sum
    of any `k` consecutive items in the resulting array does not exceed
    `threshold`.

    This is a placeholder — students should replace the body with their
    implementation. The tests will import this function and call it directly.
    """
    # TODO: implement the algorithm and return an integer (number of removals)
    return 0

print(reduce_gifts([3, 2, 1, 4, 6, 5], 3, 14))  # Example usage